import React from 'react';

import { Text } from './TestComponent.styles';
import colors from 'styles/colors';

const TestComponent = () => {
  return <Text backgroundColor={colors.black}>Test Component</Text>;
};

export default TestComponent;
